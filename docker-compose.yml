version: '3.8'

services:
  back:
    build: ./back
    container_name: back-web-1
    restart: always
    ports:
      - "8000:8000"
    env_file: ./back/.env

  front:
    build: ./web
    container_name: front-web-1
    restart: always
    ports:
      - "80:80"  
    depends_on:
      - back